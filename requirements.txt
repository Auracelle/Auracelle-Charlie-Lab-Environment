# ── Auracelle Charlie — Python dependencies ───────────────────────────────────
# Pin major versions; patch versions left open for security updates.
# Install: pip install -r requirements.txt

# ── Core Streamlit ────────────────────────────────────────────────────────────
streamlit>=1.35,<2.0

# ── Data science ─────────────────────────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0

# ── Visualisation ────────────────────────────────────────────────────────────
plotly>=5.18,<6.0
matplotlib>=3.8,<4.0
networkx>=3.2,<4.0

# ── World Bank API ────────────────────────────────────────────────────────────
wbgapi>=1.0.12,<2.0

# ── HTTP ─────────────────────────────────────────────────────────────────────
requests>=2.31,<3.0

# ── YAML (config) ────────────────────────────────────────────────────────────
PyYAML>=6.0,<7.0

# ── Document parsing (Policy Stress Testing document upload) ──────────────────
PyPDF2>=3.0,<4.0
python-docx>=1.1,<2.0

# ── Optional: agentic AI backend ──────────────────────────────────────────────
# openai>=1.30,<2.0        # Uncomment to enable GPT-4o-mini Policy Owner / Red Team
# fastapi>=0.111,<1.0      # Uncomment to run the research FastAPI backend
# uvicorn[standard]>=0.29  # Uncomment with FastAPI
# pydantic>=2.7,<3.0       # Uncomment with FastAPI
# nest-asyncio>=1.6        # Uncomment if running FastAPI inside Streamlit process

# ── Optional: SIPRI CSV parsing ───────────────────────────────────────────────
# openpyxl>=3.1            # Uncomment if SIPRI data comes as Excel

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
